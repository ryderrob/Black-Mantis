# This file makes Python treat the `bot` directory as a package.

# Optionally, you can define what gets imported when someone does `from bot import *`
# For example:
# from .main import main_function
# from .db import SessionLocal

# Or define package-level variables:
# VERSION = "0.1.0"

# For now, it can remain empty if not immediately needed for imports.
